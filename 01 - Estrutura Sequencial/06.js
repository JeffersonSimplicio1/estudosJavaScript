const raio = prompt("Qual o valor do raio da circunferencia?");
const valorRaio = Number(raio);

const pi = Number(3.14);

const areaCirculo = pi * (valorRaio * valorRaio);

window.alert("A area do circulo é de: " + areaCirculo);